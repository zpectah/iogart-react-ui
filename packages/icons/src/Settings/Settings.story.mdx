import { Meta, Story } from '@storybook/addon-docs';

import Settings from './Settings';
export const Template = (args) => <Settings {...args} />;

<Meta title="Icons" component={Settings} />

# Settings

`@iogart-react-ui/icons/Settings`

```javascript
import { Settings } from 'iogart-react-ui/icons';
```

<Story name="Settings" args={{}}>
  {Template.bind({})}
</Story>
