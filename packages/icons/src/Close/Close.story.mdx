import { Meta, Story } from '@storybook/addon-docs';

import Close from './Close';
export const Template = (args) => <Close {...args} />;

<Meta title="Icons" component={Close} />

# Close

`@iogart-react-ui/icons/Close`

```javascript
import { Close } from 'iogart-react-ui/icons';
```

<Story name="Close" args={{}}>
  {Template.bind({})}
</Story>
