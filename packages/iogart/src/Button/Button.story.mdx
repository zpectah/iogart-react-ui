import { Meta, Story } from '@storybook/addon-docs';

import Button from './Button';
export const Template = (args) => <Button {...args} />;

<Meta title="Components/Action/Button" component={Button} />

# Button

`@iogart-react-ui/iogart/Button`

```javascript
import { Button } from 'iogart-react-ui/iogart';
```

<Story
  name="Default"
  args={{
    children: 'Default Button',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Primary"
  args={{
    children: 'Primary Button',
    color: 'primary',
    variant: 'contained',
    disabled: false,
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Secondary"
  args={{
    children: 'Secondary Button',
    color: 'secondary',
    variant: 'contained',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Success"
  args={{
    children: 'Success Button',
    color: 'success',
    variant: 'contained',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Info"
  args={{
    children: 'Info Button',
    color: 'info',
    variant: 'contained',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Warning"
  args={{
    children: 'Warning Button',
    color: 'warning',
    variant: 'contained',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Error"
  args={{
    children: 'Error Button',
    color: 'error',
    variant: 'contained',
  }}
>
  {Template.bind({})}
</Story>
