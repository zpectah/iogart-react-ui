import { Meta, Story } from '@storybook/addon-docs';

import Collapsible from './Collapsible';
export const Template = (args) => <Collapsible {...args} />;

<Meta
    title="Utils/Collapsible"
    component={Collapsible}
/>

# Collapsible
`@iogart-react-ui/utils/Collapsible`

```javascript
import { Collapsible } from 'iogart-react-ui/utils';
```

<Story
  name="Collapsible"
  args={{
    children: (
        <>some content collapsible block</>
    ),
    renderToggle: ({ collapsed, toggleProps }) => (
        <button {...toggleProps}>toggle is {collapsed ? 'open' : 'closed'}</button>
    ),
  }}>
  {Template.bind({})}
</Story>
