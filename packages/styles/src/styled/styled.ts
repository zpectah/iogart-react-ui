import {
    css,
    withTheme,
} from '@emotion/react';
import {
    default as emoStyled,
} from '@emotion/styled';

// import { Theme } from '@iogart-react-ui/types/src';

const styled = emoStyled;

export {
    withTheme,
    styled,
    css,
};
