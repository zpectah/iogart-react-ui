export enum transitionEasingKeys {
  easeInOut = 'easeInOut',
  easeOut = 'easeOut',
  easeIn = 'easeIn',
  sharp = 'sharp',
}

export enum transitionDurationKeys {
  slower = 'slower',
  slow = 'slow',
  standard = 'standard',
  fast = 'fast',
}
