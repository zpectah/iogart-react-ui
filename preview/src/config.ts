import previewsConfig from './previews.config';

const config = {
  globals: {
    meta: {
      title: 'Iogart preview',
      version: '0.0.0',
    },
  },
  previews: previewsConfig,
};

export default config;
