import { createUseStyles } from 'react-jss';

export const useDashboardSearchStyles = createUseStyles({
  root: {
    width: '100%',
    height: 'auto',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    gap: '1rem',
  },
});
